import React from "react";
import ReactDOM from "react-dom/client";
import MSLSearchApp from "./MSLSearchApp";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <MSLSearchApp />
  </React.StrictMode>
);